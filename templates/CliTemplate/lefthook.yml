pre-commit:
  parallel: false # Ensures commands run sequentially to catch errors properly
  scripts:
    lint:
      # Run ESLint linting command
      run: 'yarn lint'
    typescript-check:
      # Run TypeScript type-checking
      run: 'npx tsc --noEmit' # Ensures TypeScript doesn't emit files
