import { StyleSheet } from 'react-native';

export const splashStyles = () =>
  StyleSheet.create({
    container: {
      alignItems: 'center',
      flex: 1,
      justifyContent: 'center',
    },
  });
