import { StyleSheet } from 'react-native';

export const networkloggerStyles = () =>
  StyleSheet.create({
    container: {
      alignItems: 'center',
      flex: 1,
      justifyContent: 'center',
    },
  });
