import {StyleSheet} from 'react-native';

export const settingsStyles = () =>
  StyleSheet.create({
    container: {
      alignItems: 'center',
      flex: 1,
      justifyContent: 'center',
    },
  });
