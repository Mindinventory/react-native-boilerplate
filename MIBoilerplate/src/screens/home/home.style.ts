import { StyleSheet } from 'react-native';

export const homeStyles = () =>
  StyleSheet.create({
    container: {
      alignItems: 'center',
      flex: 1,
      justifyContent: 'center',
    },
  });
